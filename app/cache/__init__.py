# Marks `app.cache` as a package.
# redis_client.py: shared Redis connection singleton.
# rate_limit.py: per-user query rate limiter.
# conversation.py: "last 5 queries" conversation-context helper (for "that email" style references).
