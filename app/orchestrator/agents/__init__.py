# Marks `app.orchestrator.agents` as a package. Each module implements the
# `ServiceAgent` interface (base.py) for one Google service: search Postgres'
# cached/embedded copy of the user's data, execute a write action via the live
# Google API, or get_context (full detail) for a specific item.
