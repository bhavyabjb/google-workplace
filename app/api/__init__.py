# Marks `app.api` as a package: FastAPI route modules (routes_query.py, routes_auth.py,
# routes_sync.py) plus deps.py, the shared dependency that resolves "which user is this
# request from" out of the session JWT.
